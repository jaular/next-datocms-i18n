export type PageProps = {
  title: string;
  button: string;
};

export type AllPostProps = {
  title: string;
  slug: string;
  description: string;
};

export type PostProps = {
  title: string;
  slug: string;
  value: string;
};
